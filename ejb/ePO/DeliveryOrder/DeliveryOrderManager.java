/*******************************************************************************
 * Copyright 1999-2015, Computer Sciences Corporation. All rights reserved.
 *  
 * Warning: This computer program is protected by copyright law and international treaties.
 * Unauthorized reproduction or distribution of this program, or any portion of it, 
 * may result in severe civil and criminal penalties, and will be prosecuted to 
 * the maximum extent possible under the law.
 ******************************************************************************/
/*
-------------------------------------------------------------------------------------------------------------------------------
Date		            Edit History	        Name		       Rev.Date		     Rev.Name			     Description
-------------------------------------------------------------------------------------------------------------------------------
06/04/2017				IN063878				Badmavathi B					        					 Unused variables
-------------------------------------------------------------------------------------------------------------------------------
*/
package ePO.DeliveryOrder;
import javax.ejb.SessionContext;
import javax.ejb.EJBException;
import eCommon.Common.*;
import ePO.Common.PoEJBSessionAdapter;
import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.ArrayList;
import java.util.Properties;
import java.util.HashMap;

/**
*
* @ejb.bean
*	name="DeliveryOrder"
*	type="Stateless"
*	transaction-type="Bean"
*	view-type="both" 
*	jndi-name="DeliveryOrder"
*	local-jndi-name="DeliveryOrder"
*	impl-class-name="ePO.DeliveryOrder.DeliveryOrderManager"
*	
*
* @ejb.interface
*	extends="javax.ejb.EJBObject"
*	local-extends="javax.ejb.EJBLocalObject" 
*	local-class="ePO.DeliveryOrder.DeliveryOrderLocal"
*	remote-class="ePO.DeliveryOrder.DeliveryOrderRemote"
*	generate= "local,remote"
*
* @ejb.home
*	extends="javax.ejb.EJBHome" 
*	local-extends="javax.ejb.EJBLocalHome"
*	local-class="ePO.DeliveryOrder.DeliveryOrderLocalHome"
*	remote-class="ePO.DeliveryOrder.DeliveryOrderHome"
*	generate= "local,remote"
*
*
*/


public class  DeliveryOrderManager extends PoEJBSessionAdapter {
//	public SessionContext context = null;
	public void setSessionContext( SessionContext sessionContext) {
		super.setSessionContext(sessionContext);
		this.context = sessionContext;
	}
	///Function for handling generic operation..
	String SQL_PO_DOC_TYPE_SELECT_FLAG_FROM_CONTROL_TABLE;	

	/**
	* @ejb.interface-method
	*	 view-type="both"
	*/

	public HashMap insert(HashMap hmData, HashMap hmSQL)	{
	
		Connection	connection = null;
		HashMap hmReturn = new HashMap();
		PreparedStatement preparedStatement	=	null;
		String sql =	"";
		ArrayList alData = null;
		ArrayList alRecord = null;
		ArrayList alDevlRecord = null;
		//ArrayList alBillDevlRecord = null; Commented for unused variable IN063878
		Properties properties = null;
		ArrayList doc_no = new ArrayList();
		String doc_no_temp = "";
		//String   trn_type		=	"PO";
		int      sizeOfTrn		=	1;
		int      trnRecords		=	1;

		boolean isUpdateSuccessful = true;
		boolean isDocNoAutoGenerated	=	false;
		//boolean isPurUnitPrefix			=	false; Commented for unused variable IN063878
		boolean isFacilityPrefix		=	false;
		boolean isYearPrefix			=	false;
		int iResult = 0;
		int failedRecordIndex = -1;
		int[] iaResult = null;
		//int iBillResult = 0;
		int[] devlResult = null;
		//int[] billdevlResult = null; Commented for unused variable IN063878
		String do_no = "";
		//int sResult = 0;
		hmReturn.put(RESULT,FALSE);
		//	ArrayList	alDtlData1			=	(ArrayList)	hmData.get("InsertDTLData");
			System.out.flush();
			//ArrayList	alStatusData			=	null;

		try {
			properties =	(Properties) hmData.get("properties");
			connection = getConnection(properties);
			connection.setAutoCommit(false);

			int records_per_trn = 0;
			

			ArrayList final_dtl=(ArrayList) hmData.get("FINAL_DTL");
			
							
			// Insert Header Value
			for(int j=0,k=0;j<sizeOfTrn;j++){
			
				alData =	(ArrayList) hmData.get("InsertHDRData");
				
				SQL_PO_DOC_TYPE_SELECT_FLAG_FROM_CONTROL_TABLE	=	(String)hmSQL.get ("SQL_PO_DOC_TYPE_SELECT_FLAG_FROM_CONTROL_TABLE");
				
				HashMap hmRecord	=	fetchRecord(connection, SQL_PO_DOC_TYPE_SELECT_FLAG_FROM_CONTROL_TABLE);
				
				
			isDocNoAutoGenerated	=	checkForNull((String)hmRecord.get("DO_DOC_NO_GEN_METHOD"),"N").equals("A");
		//	isPurUnitPrefix			=	checkForNull((String)hmRecord.get("PO_PURUNIT_PREFIX_YN"),"N").equals("Y");
			isFacilityPrefix		=	checkForNull((String)hmRecord.get("DO_FACILITY_PREFIX_YN"),"N").equals("Y");
			isYearPrefix			=	checkForNull((String)hmRecord.get("DO_YEAR_PREFIX_YN"),"N").equals("Y");
			
			
			// Insert Header Value
			
				if (isDocNoAutoGenerated) {
				//	if(isPurUnitPrefix){
				//		doc_no_temp = (String)alData.get(2);
				//	}
					if(isFacilityPrefix){
						doc_no_temp = doc_no_temp +  (String)alData.get(1);
					}
					if(isYearPrefix){
						doc_no_temp = doc_no_temp +  (String)fetchRecord(connection, SQL_PO_DOC_TYPE_SELECT_FLAG_FROM_CONTROL_TABLE).get("CURRENT_YEAR");
					}
				//	doc_no_temp = 	doc_no_temp + (String)fetchRecord(connection, SQL_PO_DOC_TYPE_SELECT_FLAG_FROM_CONTROL_TABLE).get("DO_NEXT_SRLNO");
					do_no = (String)fetchRecord(connection, SQL_PO_DOC_TYPE_SELECT_FLAG_FROM_CONTROL_TABLE).get("DO_NEXT_SRLNO") ; 
					if( do_no != null && do_no != ""){
						doc_no_temp = 	doc_no_temp + do_no;
					}else{
						hmReturn.put(RESULT,FALSE);
						hmReturn.put(MSGID,DOCNO_NULL);
						return hmReturn;
												
					}	
				}else{
				doc_no_temp=(String)alData.get(0);		
				}

				alData.set(0,doc_no_temp);
			
				doc_no.add((String)alData.get(0));
				sql = (String) hmSQL.get	("InsertHDRSQL");
				
				preparedStatement = connection.prepareStatement(sql);
				setData(preparedStatement,alData);
				iResult = preparedStatement.executeUpdate();
				if(preparedStatement!=null)
						preparedStatement.close();
				if(iResult != 0) {
					hmReturn.put(RESULT,TRUE);
					hmReturn.put(MSGID,RECORD_INSERTED);
				}
				else {
					connection.rollback();
					throw new EJBException("Insert failed in header");
				}

				// Insert Detail Value
				sql = null;
				alData =null;
		
				
				alData = final_dtl;
				sql = (String)	hmSQL.get	("InsertDTLSQL");
				preparedStatement = connection.prepareStatement(sql);
				trnRecords = alData.size();
			
				for (int index = k; index < trnRecords; index++) {
					alRecord = (java.util.ArrayList) alData.get(index);
					alRecord.set(0,doc_no_temp);
					setData(preparedStatement, alRecord);
					preparedStatement.addBatch();
				}
				try{
				iaResult = preparedStatement.executeBatch();
				}catch(Exception exception){
				hmReturn.put(RESULT,FALSE);
				hmReturn.put(MSGID,exception.toString());
				connection.rollback();
				return hmReturn;
				}
				if(preparedStatement!=null)
						preparedStatement.close();
				for (int i=0;i<iaResult.length ;i++){
					if((iaResult[i] != -2) && (iaResult[i]<0)){
						failedRecordIndex = i;
						isUpdateSuccessful = false;
						break;
					}	
				}
				if(!isUpdateSuccessful){
					connection.rollback();
					throw new EJBException("Update failed while inserting detail record " + failedRecordIndex);
				}
				
				// Insert UNF Detail Value
				if (hmData.containsKey("UNF_DTL") && hmSQL.containsKey("InsertUNFDTLSQL")) {				
				ArrayList unf_dtl=(ArrayList) hmData.get("UNF_DTL");
				
				sql = null;
				alData =null;
				
				
				alData = unf_dtl;
				sql = (String)	hmSQL.get	("InsertUNFDTLSQL");
				preparedStatement = connection.prepareStatement(sql);
				trnRecords = alData.size();
			
				for (int index = k; index < trnRecords; index++) {
					alRecord = (java.util.ArrayList) alData.get(index);
					alRecord.set(0,doc_no_temp);
					setData(preparedStatement, alRecord);
					preparedStatement.addBatch();
				}
				try{
				iaResult = preparedStatement.executeBatch();
				}catch(Exception exception){
				hmReturn.put(RESULT,FALSE);
				hmReturn.put(MSGID,exception.toString());
				connection.rollback();
				return hmReturn;
				}
				if(preparedStatement!=null)
						preparedStatement.close();
				for (int i=0;i<iaResult.length ;i++){
					if((iaResult[i] != -2) && (iaResult[i]<0)){
						failedRecordIndex = i;
						isUpdateSuccessful = false;
						break;
					}	
				}
				if(!isUpdateSuccessful){
					connection.rollback();
					throw new EJBException("Update failed while inserting detail record " + failedRecordIndex);
				}
				
			}	
				
				
			// Insert Batch Details
				
			if (hmData.containsKey("InsertBATCHData") && hmSQL.containsKey("InsertBATCHSQL")) {						
				sql = null;
				alData =null;
		
				alData =	(ArrayList) hmData.get("InsertBATCHData");

				if(alData.size()>0){
				sql = (String)	hmSQL.get	("InsertBATCHSQL");
				preparedStatement = connection.prepareStatement(sql);
				trnRecords = alData.size();
			
				for (int index = k; index < trnRecords; index++) {
					alDevlRecord = (java.util.ArrayList) alData.get(index);
					alDevlRecord.set(0,doc_no_temp);
					setData(preparedStatement, alDevlRecord);
					preparedStatement.addBatch();
				}
				try{
				devlResult = preparedStatement.executeBatch();
				}catch(Exception exception){
				hmReturn.put(RESULT,FALSE);
				hmReturn.put(MSGID,exception.toString());
				connection.rollback();
				return hmReturn;
				}
				if(preparedStatement!=null)
						preparedStatement.close();
				for (int i=0;i<devlResult.length ;i++){
					if((devlResult[i] != -2) && (devlResult[i]<0)){
						failedRecordIndex = i;
						isUpdateSuccessful = false;
						break;
					}	
				}
				if(!isUpdateSuccessful){
					connection.rollback();
					throw new EJBException("Update failed while inserting Delivery detail record " + failedRecordIndex);
				}
				}
			}	
			
			
			// Update PO_ORDER_DELV_SCH Finalized NO
			if (hmData.containsKey("UpdateDELVSCHDataNO") && hmSQL.containsKey("UpdateDELVSCHSQLNO")) {				
				ArrayList req_dtl=(ArrayList) hmData.get("UpdateDELVSCHDataNO");
				sql = null;
				alData =null;
				
				
				alData = req_dtl;
				sql = (String)	hmSQL.get	("UpdateDELVSCHSQLNO");
				preparedStatement = connection.prepareStatement(sql);
				trnRecords = alData.size();
			
				for (int index = k; index < trnRecords; index++) {
					alRecord = (java.util.ArrayList) alData.get(index);
					alRecord.set(0,doc_no_temp);
					setData(preparedStatement, alRecord);
					preparedStatement.addBatch();
				}
				try{
				iaResult = preparedStatement.executeBatch();
				}catch(Exception exception){
				hmReturn.put(RESULT,FALSE);
				hmReturn.put(MSGID,exception.toString());
				connection.rollback();
				return hmReturn;
				}
				if(preparedStatement!=null)
						preparedStatement.close();
				for (int i=0;i<iaResult.length ;i++){
					if((iaResult[i] != -2) && (iaResult[i]<0)){
						failedRecordIndex = i;
						isUpdateSuccessful = false;
						break;
					}	
				}
				if(!isUpdateSuccessful){
					connection.rollback();
					throw new EJBException("Update failed while updating PO_ORDER_DELV_SCH record " + failedRecordIndex);
				}
				
			}	
				// Update PO_ORDER_DELV_SCH
			if (hmData.containsKey("UpdateDELVSCHData") && hmSQL.containsKey("UpdateDELVSCHSQL")) {				
				ArrayList req_dtl=(ArrayList) hmData.get("UpdateDELVSCHData");
				sql = null;
				alData =null;
				
				
				alData = req_dtl;
				sql = (String)	hmSQL.get	("UpdateDELVSCHSQL");
				preparedStatement = connection.prepareStatement(sql);
				trnRecords = alData.size();
			
				for (int index = k; index < trnRecords; index++) {
					alRecord = (java.util.ArrayList) alData.get(index);
					alRecord.set(0,doc_no_temp);
					setData(preparedStatement, alRecord);
					preparedStatement.addBatch();
				}
				try{
				iaResult = preparedStatement.executeBatch();
				}catch(Exception exception){
				hmReturn.put(RESULT,FALSE);
				hmReturn.put(MSGID,exception.toString());
				connection.rollback();
				return hmReturn;
				}
				if(preparedStatement!=null)
						preparedStatement.close();
				for (int i=0;i<iaResult.length ;i++){
					if((iaResult[i] != -2) && (iaResult[i]<0)){
						failedRecordIndex = i;
						isUpdateSuccessful = false;
						break;
					}	
				}
				if(!isUpdateSuccessful){
					connection.rollback();
					throw new EJBException("Update failed while updating PO_ORDER_DELV_SCH record " + failedRecordIndex);
				}
				
			}	
			
			// Update PO_ORDER_FCY_DTL
			if (hmData.containsKey("UpdatePODTLData") && hmSQL.containsKey("UpdatePODTLSQL")) {				
				ArrayList req_dtl=(ArrayList) hmData.get("UpdatePODTLData");
				sql = null;
				alData =null;
				
				
				alData = req_dtl;
				sql = (String)	hmSQL.get	("UpdatePODTLSQL");
				preparedStatement = connection.prepareStatement(sql);
				trnRecords = alData.size();
			
				for (int index = k; index < trnRecords; index++) {
					alRecord = (java.util.ArrayList) alData.get(index);
					setData(preparedStatement, alRecord);
					preparedStatement.addBatch();
				}
				try{
				iaResult = preparedStatement.executeBatch();
				}catch(Exception exception){
				hmReturn.put(RESULT,FALSE);
				hmReturn.put(MSGID,exception.toString());
				connection.rollback();
				return hmReturn;
				}
				if(preparedStatement!=null)
						preparedStatement.close();
				for (int i=0;i<iaResult.length ;i++){
					if((iaResult[i] != -2) && (iaResult[i]<0)){
						failedRecordIndex = i;
						isUpdateSuccessful = false;
						break;
					}	
				}
				if(!isUpdateSuccessful){
					connection.rollback();
					throw new EJBException("Update failed while updating PO_ORDER_DELV_SCH record " + failedRecordIndex);
				}
				
			}	
			
			
			// Update PO_ORDER_HDR
			if (hmData.containsKey("UpdatePOHDRData") && hmSQL.containsKey("UpdatePOHDRSQL")) {				
				ArrayList req_dtl=(ArrayList) hmData.get("UpdatePOHDRData");
				sql = null;
				alData =null;
				
				
				alData = req_dtl;
				sql = (String)	hmSQL.get	("UpdatePOHDRSQL");
				preparedStatement = connection.prepareStatement(sql);
				trnRecords = alData.size();
			
				for (int index = k; index < trnRecords; index++) {
					alRecord = (java.util.ArrayList) alData.get(index);
					setData(preparedStatement, alRecord);
					preparedStatement.addBatch();
				}
				try{
				iaResult = preparedStatement.executeBatch();
				}catch(Exception exception){
				hmReturn.put(RESULT,FALSE);
				hmReturn.put(MSGID,exception.toString());
				connection.rollback();
				return hmReturn;
				}
				if(preparedStatement!=null)
						preparedStatement.close();
				for (int i=0;i<iaResult.length ;i++){
					if((iaResult[i] != -2) && (iaResult[i]<0)){
						failedRecordIndex = i;
						isUpdateSuccessful = false;
						break;
					}	
				}
				if(!isUpdateSuccessful){
					connection.rollback();
					throw new EJBException("Update failed while updating PO_ORDER_DELV_SCH record " + failedRecordIndex);
				}
				
			}	
				
	
				iaResult = null;
				sql = null;
				alData = null;
			
						
				//Update the document no
				ArrayList allanguageData			=	(ArrayList)	hmData.get("LANGUAGE_DATA");
				String locale=(String)allanguageData.get(0);
				if (isDocNoAutoGenerated) {
					String s  = "";
					String s1 = "";
					sql = (String)	hmSQL.get	("UpdateSQL");
					alData = (ArrayList)	hmData.get ("UpdateData");
					preparedStatement = connection.prepareStatement(sql);
					setData(preparedStatement,alData);
					iResult = preparedStatement.executeUpdate();
					if(doc_no.size()>1){
						java.util.Locale loc = new java.util.Locale(locale);
						java.util.ResourceBundle common_labels = java.util.ResourceBundle.getBundle( "eCommon.resources.Labels",loc);
						s = getPOMessage(locale, "DOC_NOS_GEN_FROM", "PO") ;
					
						s1=s+"  "+doc_no.get(0)+" "+common_labels.getString("Common.to.label")+" "+doc_no.get(doc_no.size()-1);
					}else{
						s = getPOMessage(locale, "DOC_NO", "PO") ;
						s1=s+":  "+doc_no.get(0);
					}
					hmReturn.put("flag",s1);
					if(preparedStatement!=null)
							preparedStatement.close();
					if(iResult<=0){
						connection.rollback();
						throw new SQLException("Update of Document No failed");
					}
				}
				
				// After successful insertion and updation 
			k=k+records_per_trn;
			}
				connection.commit();
				hmReturn.put(RESULT,TRUE);
				hmReturn.put(MSGID,RECORD_INSERTED);
				if (connection!=null) {
						closeConnection(connection,properties);	
				}
		}catch(SQLException sqlException) {
			try {
				connection.rollback();
				hmReturn.put(RESULT,FALSE);
				if(sqlException.getErrorCode()==1) {
					hmReturn.put(MESSAGE,CODE_ALREADY_EXISTS);
					hmReturn.put(MSGID,CODE_ALREADY_EXISTS);

				}
				else {
					hmReturn.put(MESSAGE,sqlException.toString());
				}
				sqlException.printStackTrace();
			}
			catch (Exception exception) {
				exception.printStackTrace(); 
			}
        }
		catch(Exception exception) {
			try {
				connection.rollback();
				hmReturn.put(RESULT,FALSE);
				hmReturn.put(MESSAGE,exception.toString());
				hmReturn.put(MSGID,exception.toString());
				exception.printStackTrace();
			}
			catch (Exception subexception) {
				subexception.printStackTrace(); 
			}
        }
		finally{
			try{
				if(preparedStatement!=null)
					preparedStatement.close();
				if (connection!=null) {
					closeConnection(connection,(Properties)hmData.get(properties));	
				}
			}
			catch (Exception exception){
				exception.printStackTrace();
			}
		} 
	
		hmReturn.put("doc_no",doc_no);

		return hmReturn;
	}

	/**
	* @ejb.interface-method
	*	 view-type="both"
	*/

	public HashMap modify(HashMap hmData, HashMap hmSQL)	 {
	
		Connection	connection = null;
		HashMap hmReturn = new HashMap();
		PreparedStatement preparedStatement	=	null;
		ArrayList alData =	new ArrayList();
		ArrayList	 alRecord =	new ArrayList();
		Properties properties = null;
		String sql =	"";
		int iResult =	0;
//		ArrayList	alStatusData			=	null;
	
		int iaResult[] = null;
	//	int sResult = 0;
		boolean isUpdateSuccessful = true;
		hmReturn.put(RESULT,FALSE);
		hmReturn.put(MESSAGE,"No records found to be modified..");
		properties =	(Properties) hmData.get("properties");
		//25/06/2012 below
		 ArrayList alDevlRecord = null;  
		 int[] devlResult = null;  
		 int failedRecordIndex = -1;
		
		try {
			connection = getConnection(properties);
			connection.setAutoCommit(false);
			
			if (hmData.containsKey("UpdateData") && hmSQL.containsKey("UpdateSQL")) {
			alData =	(ArrayList)	hmData.get ("UpdateData");
			sql = (String)	hmSQL.get	("UpdateSQL");
			preparedStatement = connection.prepareStatement(sql);
			setData(preparedStatement,alData);
			iResult	 =	preparedStatement.executeUpdate();
			if(preparedStatement!=null)
					preparedStatement.close();
			if(iResult!=0){
				hmReturn.put(RESULT,TRUE);
				hmReturn.put(MSGID,RECORD_MODIFIED);
			}
			}
			alData = null;
			sql = null;
			
			//Update Bill dvly detail data
		if (hmData.containsKey("UpdateBillDvlyData") && hmSQL.containsKey("UpdateBillDvlySQL")) {	
			alData =	(ArrayList)	hmData.get ("UpdateBillDvlyData");
			sql = (String)	hmSQL.get	("UpdateBillDvlySQL");
		
				if (alData.size() >0) {
					preparedStatement = connection.prepareStatement(sql);
					for (int index = 0; index < alData.size(); index++) {
						alRecord = (java.util.ArrayList) alData.get(index);
						setData(preparedStatement, alRecord);
						preparedStatement.addBatch();
					}
					iaResult = preparedStatement.executeBatch();
					if(preparedStatement!=null)
					preparedStatement.close();
					for (int i=0;i<iaResult.length ;i++){
						if((iaResult[i] != -2) && (iaResult[i]<0)){
							isUpdateSuccessful = false;
							break;
						}	
					}
					if(!isUpdateSuccessful){
						connection.rollback();
						throw new EJBException("Update failed while inserting detail record");
					}
				}
			
			}
			
				//Insert Bill dvly detail data
		if (hmData.containsKey("InsertBillDvlyData") && hmSQL.containsKey("InsertBillDvlySQL")) {	
			alData =	(ArrayList)	hmData.get ("InsertBillDvlyData");
			sql = (String)	hmSQL.get	("InsertBillDvlySQL");
		
				if (alData.size() >0) {
					preparedStatement = connection.prepareStatement(sql);
					for (int index = 0; index < alData.size(); index++) {
						alRecord = (java.util.ArrayList) alData.get(index);
						setData(preparedStatement, alRecord);
						preparedStatement.addBatch();
					}
					iaResult = preparedStatement.executeBatch();
					if(preparedStatement!=null)
					preparedStatement.close();
					for (int i=0;i<iaResult.length ;i++){
						if((iaResult[i] != -2) && (iaResult[i]<0)){
							isUpdateSuccessful = false;
							break;
						}	
					}
					if(!isUpdateSuccessful){
						connection.rollback();
						throw new EJBException("Update failed while inserting detail record");
					}
				}
			
			}
			
			
					// delete Batch Detail Value
			alData = null;
			sql = null; 
			
			
			if (hmData.containsKey("DeleteDevlDTLData") && hmSQL.containsKey("DeleteDevlDTLSQL")) {
				alData = (ArrayList)	hmData.get ("DeleteDevlDTLData");
				sql = (String)	hmSQL.get	("DeleteDevlDTLSQL");
				if (alData.size() >0) {
					preparedStatement = connection.prepareStatement(sql);
					for (int index = 0; index < alData.size(); index++) {
						alDevlRecord = (java.util.ArrayList) alData.get(index);
						setData(preparedStatement, alDevlRecord);
						preparedStatement.addBatch();
					}
					devlResult = preparedStatement.executeBatch();
					if(preparedStatement!=null)
					preparedStatement.close();
					for (int i=0;i<devlResult.length ;i++){
						if((devlResult[i] != -2) && (devlResult[i]<0)){
							isUpdateSuccessful = false;
							break;
						}	
					}
					if(!isUpdateSuccessful){
						connection.rollback();
						throw new EJBException("Update failed while inserting detail record");
					}
				}
			}
			
			// delete dtl data
			
			alData = null;
			sql = null;
			
			
			if (hmData.containsKey("DeleteDTLData") && hmSQL.containsKey("DeleteDTLSQL")) {
				alData = (ArrayList)	hmData.get ("DeleteDTLData");
				sql = (String)	hmSQL.get	("DeleteDTLSQL");
				if (alData.size() >0) {
					preparedStatement = connection.prepareStatement(sql);
					for (int index = 0; index < alData.size(); index++) {
						alRecord = (java.util.ArrayList) alData.get(index);
						setData(preparedStatement, alRecord);
						preparedStatement.addBatch();
					}
					iaResult = preparedStatement.executeBatch();
					if(preparedStatement!=null)
					preparedStatement.close();
					for (int i=0;i<iaResult.length ;i++){
						if((iaResult[i] != -2) && (iaResult[i]<0)){
							isUpdateSuccessful = false;
							break;
						}	
					}
					if(!isUpdateSuccessful){
						connection.rollback();
						throw new EJBException("Update failed while inserting detail record");
					}
				}
			}

			// Added by Sakti to UPDATE BLOCKED QTY DLV SCH TABLE data IF CHECK BOX IS UNCHECKED inc#41083
			
			alData = null;
			sql = null;
			
			
			if (hmData.containsKey("DeleteSchdlvData") && hmSQL.containsKey("DeleteSchdlvSQL")) {
				alData = (ArrayList)	hmData.get ("DeleteSchdlvData");
				sql = (String)	hmSQL.get	("DeleteSchdlvSQL"); 
				if (alData.size() >0) {
					preparedStatement = connection.prepareStatement(sql);
					for (int index = 0; index < alData.size(); index++) {
						alRecord = (java.util.ArrayList) alData.get(index);
						setData(preparedStatement, alRecord);
						preparedStatement.addBatch();
					}
					iaResult = preparedStatement.executeBatch();
					if(preparedStatement!=null)
					preparedStatement.close();
					for (int i=0;i<iaResult.length ;i++){
						if((iaResult[i] != -2) && (iaResult[i]<0)){
							isUpdateSuccessful = false;
							break;
						}	
					}
					if(!isUpdateSuccessful){
						connection.rollback();
						throw new EJBException("Update failed while inserting detail record");
					}
				}
			}//Added ends
			
			// Update Commitment Code Balance Budget Amount
			
			alData = null;
			sql = null;
			
			
			if (hmData.containsKey("UpdateCOMMTData") && hmSQL.containsKey("UpdateCOMMTSQL")) {
				alData = (ArrayList)	hmData.get ("UpdateCOMMTData");
				sql = (String)	hmSQL.get	("UpdateCOMMTSQL");
				if (alData.size() >0) {
					preparedStatement = connection.prepareStatement(sql);
					for (int index = 0; index < alData.size(); index++) {
						alRecord = (java.util.ArrayList) alData.get(index);
						setData(preparedStatement, alRecord);
						preparedStatement.addBatch();
					}
					iaResult = preparedStatement.executeBatch();
					if(preparedStatement!=null)
					preparedStatement.close();
					for (int i=0;i<iaResult.length ;i++){
						if((iaResult[i] != -2) && (iaResult[i]<0)){
							isUpdateSuccessful = false;
							break;
						}	
					}
					if(!isUpdateSuccessful){
						connection.rollback();
						throw new EJBException("Update failed while inserting detail record");
					}
				}
			}
			
			
	// Update Commitment Code Blocked Budget Amount
			
			alData = null;
			sql = null;
			
			
			if (hmData.containsKey("UpdateBlockData") && hmSQL.containsKey("UpdateBlockSQL")) {
				alData = (ArrayList)	hmData.get ("UpdateBlockData");
				sql = (String)	hmSQL.get	("UpdateBlockSQL");
				if (alData.size() >0) {
					preparedStatement = connection.prepareStatement(sql);
					for (int index = 0; index < alData.size(); index++) {
						alRecord = (java.util.ArrayList) alData.get(index);
						setData(preparedStatement, alRecord);
						preparedStatement.addBatch();
					}
					iaResult = preparedStatement.executeBatch();
					if(preparedStatement!=null)
					preparedStatement.close();
					for (int i=0;i<iaResult.length ;i++){
						if((iaResult[i] != -2) && (iaResult[i]<0)){
							isUpdateSuccessful = false;
							break;
						}	
					}
					if(!isUpdateSuccessful){
						connection.rollback();
						throw new EJBException("Update failed while inserting detail record");
					}
				}
			}			
			
			// Update MM_ITEM 
			
						
			alData = null;
			sql = null;
			
			
			if (hmData.containsKey("UpdateMMITEMData") && hmSQL.containsKey("UpdateMMITEMSQL")) {
				alData = (ArrayList)	hmData.get ("UpdateMMITEMData");
				sql = (String)	hmSQL.get	("UpdateMMITEMSQL");
				if (alData.size() >0) {
					preparedStatement = connection.prepareStatement(sql);
					for (int index = 0; index < alData.size(); index++) {
						alRecord = (java.util.ArrayList) alData.get(index);
						setData(preparedStatement, alRecord);
						preparedStatement.addBatch();
					}
					iaResult = preparedStatement.executeBatch();
					if(preparedStatement!=null)
					preparedStatement.close();
					for (int i=0;i<iaResult.length ;i++){
						if((iaResult[i] != -2) && (iaResult[i]<0)){
							isUpdateSuccessful = false;
							break;
						}	
					}
					if(!isUpdateSuccessful){
						connection.rollback();
						throw new EJBException("Update failed while inserting detail record");
					}
				}
			}
				
				
				// Update Request Detail Value
				
			alData = null;
			sql = null;
			
			
			if (hmData.containsKey("UpdateREQDTLData") && hmSQL.containsKey("UpdateREQDTLSQL")) {
				alData = (ArrayList)	hmData.get ("UpdateREQDTLData");
				sql = (String)	hmSQL.get	("UpdateREQDTLSQL");
				if (alData.size() >0) {
					preparedStatement = connection.prepareStatement(sql);
					for (int index = 0; index < alData.size(); index++) {
						alRecord = (java.util.ArrayList) alData.get(index);
						setData(preparedStatement, alRecord);
						preparedStatement.addBatch();
					}
					iaResult = preparedStatement.executeBatch();
					if(preparedStatement!=null)
					preparedStatement.close();
					for (int i=0;i<iaResult.length ;i++){
						if((iaResult[i] != -2) && (iaResult[i]<0)){
							isUpdateSuccessful = false;
							break;
						}	
					}
					if(!isUpdateSuccessful){
						connection.rollback();
						throw new EJBException("Update failed while inserting detail record");
					}
				}
			}
				
				
				
				// Update PO_ORDER_HDR Value
				
			alData = null;
			sql = null;
			
			
			if (hmData.containsKey("UpdatePOHDRData") && hmSQL.containsKey("UpdatePOHDRSQL")) {
				alData = (ArrayList)	hmData.get ("UpdatePOHDRData");
				sql = (String)	hmSQL.get	("UpdatePOHDRSQL");
				if (alData.size() >0) {
					preparedStatement = connection.prepareStatement(sql);
					for (int index = 0; index < alData.size(); index++) {
						alRecord = (java.util.ArrayList) alData.get(index);
						setData(preparedStatement, alRecord);
						preparedStatement.addBatch();
					}
					iaResult = preparedStatement.executeBatch();
					if(preparedStatement!=null)
					preparedStatement.close();
					for (int i=0;i<iaResult.length ;i++){
						if((iaResult[i] != -2) && (iaResult[i]<0)){
							isUpdateSuccessful = false;
							break;
						}	
					}
					if(!isUpdateSuccessful){
						connection.rollback();
						throw new EJBException("Update failed while inserting detail record");
					}
				}
			}

				// Update PO_ORDER_FCY_DTL Value
				
			alData = null;
			sql = null;
			
			
			if (hmData.containsKey("UpdatePODTLData") && hmSQL.containsKey("UpdatePODTLSQL")) {
				alData = (ArrayList)	hmData.get ("UpdatePODTLData");
				sql = (String)	hmSQL.get	("UpdatePODTLSQL");
				if (alData.size() >0) {
					preparedStatement = connection.prepareStatement(sql);
					for (int index = 0; index < alData.size(); index++) {
						alRecord = (java.util.ArrayList) alData.get(index);
						setData(preparedStatement, alRecord);
						preparedStatement.addBatch();
					}
					iaResult = preparedStatement.executeBatch();
					if(preparedStatement!=null)
					preparedStatement.close();
					for (int i=0;i<iaResult.length ;i++){
						if((iaResult[i] != -2) && (iaResult[i]<0)){
							isUpdateSuccessful = false;
							break;
						}	
					}
					if(!isUpdateSuccessful){
						connection.rollback();
						throw new EJBException("Update failed while inserting detail record");
					}
				}
			}
	
				// Update PO_ORDER_DELV_SCH Value
				
			alData = null;
			sql = null;
			
			
			if (hmData.containsKey("UpdateDELVSCHData") && hmSQL.containsKey("UpdateDELVSCHSQL")) {
				alData = (ArrayList)	hmData.get ("UpdateDELVSCHData");
				sql = (String)	hmSQL.get	("UpdateDELVSCHSQL");
				if (alData.size() >0) {
					preparedStatement = connection.prepareStatement(sql);
					for (int index = 0; index < alData.size(); index++) {
						alRecord = (java.util.ArrayList) alData.get(index);
						setData(preparedStatement, alRecord);
						preparedStatement.addBatch();
					}
					iaResult = preparedStatement.executeBatch();
					if(preparedStatement!=null)
					preparedStatement.close();
					for (int i=0;i<iaResult.length ;i++){
						if((iaResult[i] != -2) && (iaResult[i]<0)){
							isUpdateSuccessful = false;
							break;
						}	
					}
					if(!isUpdateSuccessful){
						connection.rollback();
						throw new EJBException("Update failed while inserting detail record");
					}
				}
			}
			
				// Update DO_BLOCKED_QTY in PO_ORDER_DELV_SCH Value
				
			alData = null;
			sql = null;
			
			
			if (hmData.containsKey("UpdateDELVSCHDataNO") && hmSQL.containsKey("UpdateDELVSCHSQLNO")) {
				alData = (ArrayList)	hmData.get ("UpdateDELVSCHDataNO");
				sql = (String)	hmSQL.get	("UpdateDELVSCHSQLNO");
				if (alData.size() >0) {
					preparedStatement = connection.prepareStatement(sql);
					for (int index = 0; index < alData.size(); index++) {
						alRecord = (java.util.ArrayList) alData.get(index);
						setData(preparedStatement, alRecord);
						preparedStatement.addBatch();
					}
					iaResult = preparedStatement.executeBatch();
					if(preparedStatement!=null)
					preparedStatement.close();
					for (int i=0;i<iaResult.length ;i++){
						if((iaResult[i] != -2) && (iaResult[i]<0)){
							isUpdateSuccessful = false;
							break;
						}	
					}
					if(!isUpdateSuccessful){
						connection.rollback();
						throw new EJBException("Update failed while inserting detail record");
					}
				}
			}
				
			// Update do detail		 		
			
			alData = null;
			sql = null;
			if (hmData.containsKey("UpdateDTLData") && hmSQL.containsKey("UpdateDTLSQL")) {
				alData = (ArrayList)	hmData.get ("UpdateDTLData");
				sql = (String)	hmSQL.get	("UpdateDTLSQL");
				if (alData.size() >0) {
					preparedStatement = connection.prepareStatement(sql);
					for (int index = 0; index < alData.size(); index++) {
						alRecord = (java.util.ArrayList) alData.get(index);
						setData(preparedStatement, alRecord);
						preparedStatement.addBatch();
					}
					iaResult = preparedStatement.executeBatch();
					if(preparedStatement!=null)
					preparedStatement.close();
					for (int i=0;i<iaResult.length ;i++) {
						if((iaResult[i] != -2) && (iaResult[i]<0)){
							isUpdateSuccessful = false;
							break;
						}	
					}
					if(!isUpdateSuccessful){
						connection.rollback();
						throw new EJBException("Update failed while inserting detail record");
					}	
				}
			}
			if (hmData.containsKey("InsertDTLData") && hmSQL.containsKey("InsertDTLSQL")) {
				alData = (ArrayList)	hmData.get ("InsertDTLData");
				sql = (String)	hmSQL.get	("InsertDTLSQL");
				preparedStatement = connection.prepareStatement(sql);
				for (int index = 0; index < alData.size(); index++) {
					alRecord = (java.util.ArrayList) alData.get(index);
					setData(preparedStatement, alRecord);
					preparedStatement.addBatch();
				}
				iaResult = preparedStatement.executeBatch();
				if(preparedStatement!=null)
					preparedStatement.close();
				//preparedStatement.clearParameters();
				for (int i=0;i<iaResult.length ;i++){
					if((iaResult[i] != -2) && (iaResult[i]<0)){
						isUpdateSuccessful = false;
						break;
					}	
				}
			}
			
		
			
			
			// Insert Delivery Detail Value
				sql = null;
				alData =null;
			if (hmData.containsKey("InsertDELVData") && hmSQL.containsKey("InsertDELVSQL")) {
				alData =	(ArrayList) hmData.get("InsertDELVData");

				if(alData.size()>0){
				sql = (String)	hmSQL.get	("InsertDELVSQL");
				preparedStatement = connection.prepareStatement(sql);
				//trnRecords = alData.size();
			
				for (int index = 0; index < alData.size(); index++) {
					alDevlRecord = (java.util.ArrayList) alData.get(index);
					setData(preparedStatement, alDevlRecord);
					preparedStatement.addBatch();
				}
				try{
				devlResult = preparedStatement.executeBatch();
				}catch(Exception exception){
				hmReturn.put(RESULT,FALSE);
				hmReturn.put(MSGID,exception.toString());
				connection.rollback();
				return hmReturn;
				}
				if(preparedStatement!=null)
						preparedStatement.close();
				for (int i=0;i<devlResult.length ;i++){
					if((devlResult[i] != -2) && (devlResult[i]<0)){
						failedRecordIndex = i;
						isUpdateSuccessful = false;
						break;
					}	
				}
				if(!isUpdateSuccessful){
					connection.rollback();
					throw new EJBException("Update failed while inserting Delivery detail record " + failedRecordIndex);
				}
				}
			}
			
		// Update Delivery Details

			
				sql = null;
				alData =null;
			if (hmData.containsKey("UpdateDELVData") && hmSQL.containsKey("UpdateDELVSQL")) {
				alData =	(ArrayList) hmData.get("UpdateDELVData");

				if(alData.size()>0){
				sql = (String)	hmSQL.get	("UpdateDELVSQL");
				preparedStatement = connection.prepareStatement(sql);
				//trnRecords = alData.size();
			
				for (int index = 0; index < alData.size(); index++) {
					alDevlRecord = (java.util.ArrayList) alData.get(index);
					setData(preparedStatement, alDevlRecord);
					preparedStatement.addBatch();
				}
				try{
				devlResult = preparedStatement.executeBatch();
				}catch(Exception exception){
				hmReturn.put(RESULT,FALSE);
				hmReturn.put(MSGID,exception.toString());
				connection.rollback();
				return hmReturn;
				}
				if(preparedStatement!=null)
						preparedStatement.close();
				for (int i=0;i<devlResult.length ;i++){
					if((devlResult[i] != -2) && (devlResult[i]<0)){
						failedRecordIndex = i;
						isUpdateSuccessful = false;
						break;
					}	
				}
				if(!isUpdateSuccessful){
					connection.rollback();
					throw new EJBException("Update failed while inserting Delivery detail record " + failedRecordIndex);
				}
				}
			}		
			
			alData = null;
			sql = null;
		
			if(isUpdateSuccessful){
				connection.commit();
				hmReturn.put(RESULT,TRUE);
				hmReturn.put(MSGID,RECORD_MODIFIED);
			}

			closeConnection(connection,properties);	
}
		catch(Exception exception) {
			try {
				connection.rollback();
				hmReturn.put(RESULT,FALSE);
				hmReturn.put(MSGID,exception.toString());
				hmReturn.put(MESSAGE,exception.toString());
				exception.printStackTrace();
			}
			catch (Exception subexception) {
				subexception.printStackTrace();
			}
        }
		finally{
 			try{
				if(preparedStatement!=null)
					preparedStatement.close();
				closeConnection(connection,(Properties)hmData.get(properties));	
			}
			catch (Exception subexception){
				subexception.printStackTrace();
			}
		}
		return hmReturn;
	}

	/**
	* @ejb.interface-method
	*	 view-type="both"
	*/

	public HashMap delete(HashMap hmData, HashMap hmSQL)	{
		Connection connection					= null;
		HashMap hmReturn						= new HashMap();
		PreparedStatement preparedStatement		= null;
		String		sql							= "";
		Properties	properties					= null;
		ArrayList	alData						= new ArrayList();
		/* ArrayList	alBlockedQtyRed 			= new ArrayList();
		HashMap		hmArgData					= new HashMap();
		HashMap		hmArgSQL					= new HashMap();
		HashMap		hmResult					= new HashMap();
		ArrayList	alBlockData					= null; */ //Commented for unused variables IN063878
		ArrayList	alRecord					= null;
		int[] iaResult							= null;
		int	  iResult							= 0;
		boolean		isUpdateSuccessful			= true;

		properties =	(Properties) hmData.get("properties");
		hmReturn.put(RESULT,FALSE);
		hmReturn.put(MESSAGE,"No record found to be deleted..");
		try {
			connection = getConnection(properties);
			connection.setAutoCommit(false);

		
			//Po block qty reduce
			
			alData  = (ArrayList)hmData.get ("alPODelvUpdate");
			sql		= (String)	hmSQL.get	("UpdatePODelvSchBlockedQtyReduce");
		
			if (alData.size() >0) {
				preparedStatement = connection.prepareStatement(sql);
				for (int index = 0; index < alData.size(); index++) {
					alRecord = (ArrayList) alData.get(index);
					setData(preparedStatement, alRecord);
					preparedStatement.addBatch();
				}
				iaResult = preparedStatement.executeBatch();
				if(preparedStatement!=null)
				preparedStatement.close();
				for (int i=0;i<iaResult.length ;i++){
					if((iaResult[i] != -2) && (iaResult[i]<0)){
						isUpdateSuccessful = false;
						break;
					}	
				}
				if(!isUpdateSuccessful){
					connection.rollback();
					throw new EJBException("Update failed while updating block qty");
				}
			}

			
			//Delete Delivery batch
			alData = (ArrayList)	hmData.get("DeleteDELVData");
			sql = (String)	hmSQL.get	("DeleteDELVSQL");
			preparedStatement = connection.prepareStatement(sql);
			setData(preparedStatement,alData);
			iResult		=	preparedStatement.executeUpdate();
			if(iResult<=0) {
				connection.rollback();
				throw new Exception("Delete failed in delivery sch !");
			}
			
			//Delete Details
			alData = (ArrayList)	hmData.get("DeleteDTLData");
			sql = (String)	hmSQL.get	("DeleteDTLSQL");
			preparedStatement = connection.prepareStatement(sql);
			setData(preparedStatement,alData);
			iResult		=	preparedStatement.executeUpdate();
			if(iResult<=0) {
				connection.rollback();
				throw new Exception("Delete failed in Detail !");
			}
			//Delete Header
			alData = (ArrayList)	hmData.get("DeleteHDRData");
			sql = (String)	hmSQL.get	("DeleteHDRSQL");
			preparedStatement = connection.prepareStatement(sql);
			setData(preparedStatement,alData);
			iResult		=	preparedStatement.executeUpdate();
			if(preparedStatement!=null)
					preparedStatement.close();
			if(iResult<=0) {
				connection.rollback();
				throw new Exception("Delete failed in header !");
			}
			
			//Delete Bill Delivery Details
		/*	alData = (ArrayList)	hmData.get("DeleteBILLDELVData");
			sql = (String)	hmSQL.get	("DeleteBILLDELVSQL");
			preparedStatement = connection.prepareStatement(sql);
			setData(preparedStatement,alData);
			iResult		=	preparedStatement.executeUpdate();
			if(iResult<=0) {
				connection.rollback();
				throw new Exception("Delete failed in bill delivery !");
			}
		*/	
			connection.commit();
			hmReturn.put(RESULT,TRUE);
			hmReturn.put(MSGID,RECORD_DELETED);
			if (connection!=null) {
						closeConnection(connection,properties);	
			}
			
		}
		catch(Exception exception) {
			try {
				connection.rollback();
				hmReturn.put(RESULT,FALSE);
				hmReturn.put(MESSAGE,exception.toString());
				exception.printStackTrace();
			}
			catch (Exception subexception) {
					subexception.printStackTrace();
			}
        }
		finally{
			try{
				if(preparedStatement!=null) {
					preparedStatement.close();
				}
				closeConnection(connection,(Properties)hmData.get(properties));	
			}
			catch (Exception exception){
				exception.printStackTrace();
			}
		}
		return hmReturn;
	}
	protected final String MESSAGE = "message";
	protected final String RESULT = "result";
	protected final Boolean TRUE = new Boolean(true);
	protected final Boolean FALSE =	new Boolean(false);
	private final String CODE_ALREADY_EXISTS = "CODE_ALREADY_EXISTS";
    private final String RECORD_INSERTED = "RECORD_INSERTED";
    private final String RECORD_MODIFIED = "RECORD_MODIFIED";
    private final String RECORD_DELETED  =	"RECORD_DELETED";
	private final String DOCNO_NULL  	=	"DOCNO_NULL";
	
}
